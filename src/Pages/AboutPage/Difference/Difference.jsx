import './Difference.css';
import SectionTitle from '../../../Components/SectionTitle/SectionTitle';

const Difference = () => {
    return (
        <section>
            <SectionTitle
            title='DIFFERENCE'
            subTitle='留学エージェントとwarpleの違い'
            ></SectionTitle>
            <div>
                <div>
                    
                </div>
            </div>
        </section>
    );
};

export default Difference;